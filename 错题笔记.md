# 算法错题笔记 — 数论因数分解题型（B 题）

本文整理自刷题中对 B 题（数论分解）的错误反思，归纳常见错误及改正建议，以便后续复习参考。

## 题目背景

题目条件为：给定正整数 `n`，存在质数 `p`、`q`，使 `n = p^2 × q` 或 `n = p × q^2`，且保证解存在。要求输出 `p q`。数据范围使得枚举到 `∛n` 即可解决。

## 常见错误

* **输出顺序颠倒。** 未分清当前遍历的因子 `i` 是平方因子还是单因子。当 `n % (i * i) == 0` 时，`i` 对应的质数是成对出现的平方因子，应输出 `(i, n / (i * i))`。否则 `i` 是一次出现的质因子 `q`，应先计算 `p = √(n / i)` 并输出 `(p, i)`。有错误的版本输出为 `i sqrt(n / i)`，顺序反了。

* **过度复杂化处理。** 一些实现中引入了线性筛表和素数列表，其实本题无需判断质数。因为题目保证解存在，枚举到 `∛n` 范围即可找到所有可能的素因子，且质数判定并不需要显式筛表。

* **浮点根号风险。** 直接使用 `sqrt(n / i)` 得到浮点数再转整数可能存在精度误差，尤其是大数情况下。应使用整数开方函数或手工校验：例如先用近似值作为起点，向下或向上调整至 `p * p = n / i` 为止。

* **数组越界。** 在利用筛法时，错误地将数组大小设置为 `limit` 而在循环中访问了索引 `limit`，导致越界。若确实使用筛，数组长度应为 `limit + 1`；但本题推荐直接枚举，不需要建立大数组。

* **忽视立方根界限。** 正确的枚举范围应满足 `i^3 ≤ n`，因为较小的质因子不会超过 `∛n`。有错误实现取到 `sqrt(n)` 范围，增加了不必要的计算。

## 正确做法要点

1. **枚举到立方根。** 从 `i = 2` 开始，枚举直到 `i * i * i <= n`。对于每个 `i`：
   - 如果 `n % (i * i) == 0`，则 `i` 是平方因子；输出 `i` 和 `n / (i * i)`。
   - 否则如果 `n % i == 0`，说明 `i` 是一次出现的质因子 `q`，另一因子 `p` 等于整数平方根 `√(n / i)`。输出 `p` 和 `i`。
   - 找到后立即返回。

2. **使用整数开方。** 写一个 `isqrt` 函数（比如二分或牛顿法），确保返回的 `p` 满足 `p * p = n / i`。这样可以避免浮点精度问题。

3. **无需素数筛。** 由于题目保证解存在，遇到的第一个满足条件的 `i` 一定对应解，判断是否为素数并无必要。

4. **注意输出顺序。** 当 `i^2` 整除 `n` 时输出 `(i, n / i^2)`；否则输出 `(√(n / i), i)`。

通过上述修正，可以避免常见错误并顺利解决此类数论因数分解问题。