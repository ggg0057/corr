# 算法错题笔记 — 完全二叉树路径计数题型（F 题）

本笔记针对 F 题（完全二叉树上以指定节点为路径中点的路径计数）总结错误点及正确思路，以便复习。题目给定完全二叉树 `1…n`，求以节点 `x` 为路径中点的简单路径数量。

## 题目概要

完全二叉树使用层序（堆式）编号：根为 1，左儿子为 `2i`，右儿子为 `2i+1`。一条简单路径的长度为所经过的节点数，中点是路径上距两端节点距离相等的节点。要求以 `x` 为中点的所有路径数量，包括 `(x, x)`（即长度 1）这条路径。

## 常见错误

* **父亲方向理解不全。** 很多解答只枚举 `x` 的祖先链 (`x/2, x/4, x/8, …`)，忽略了从祖先拐入兄弟子树的路径。例如 `x = 4` 时，距离为 2 的父亲方向点包括祖先 `1` 和 `x` 的父亲 `2` 的兄弟子树节点 `5`，不仅仅是祖先链。

* **左右子树写错。** 在完全二叉树中，左儿子是 `2*i`，右儿子是 `2*i + 1`。错误实现中有两次写成 `2*i + 1`，导致左右子树统计相同。

* **不更新状态。** 在父亲方向循环中，需要更新 `child = fa`，否则会停留在同一层无法向上推进。同类错误也出现在使用 `last` 变量更新时。

* **引用传值混用。** 函数形参若传值（例如 `vector<int> v`），修改不会反映回原数组，应传引用 `vector<int>& v`。

* **使用大数组。** 题目中 `n` 可达 `10^9`，误用 `vector.resize(n)` 会直接爆内存。正确做法是按树的高度（约 30～60 层）开数组，用下标表示路径距离。

* **忽略单点路径。** 不少代码忘记计入 `(x, x)` 这条路径，导致少 1；答案应从 1 开始累加其它路径贡献。

* **过早优化导致逻辑复杂。** 有些解为了少计算把三个方向的数量存数组并进行偏移，这样需要处理大量索引和传参，容易出错。实际高度很小，可以直接枚举距离 `d`，逐次计算并累加，更加简洁可靠。

## 正确做法

1. **三方向划分。** 删除节点 `x` 后，树被划分为三条相互不交的方向：左子树（以 `2*x` 为根）、右子树（以 `2*x+1` 为根）和父亲方向（指向 `x` 的父亲及其兄弟子树）。以 `x` 为中点的路径两端必须来自不同方向，并且距离到 `x` 相同。

2. **统计子树方向。** 使用函数 `countDown(u, dep)` 统计节点 `u` 往下走 `dep` 条边能到达的节点数：对应编号区间为 `[u·2^dep, u·2^dep + 2^dep - 1]`，超出 `n` 时取裁剪。`dep = d-1` 时，从左/右儿子走 `d-1` 步即可到达距离 `x` 为 `d` 的节点。

3. **统计父亲方向。** 从 `x` 向上枚举祖先。假设已经向上走了 `up` 步到达 `fa`：
   - 当 `up == d` 时，祖先 `fa` 本身距离 `x` 为 `d`，贡献 1。
   - 当 `up < d` 时，还可从 `fa` 的兄弟子树下走 `d-up-1` 步。兄弟子树根为 `fa*2 + 1`（如果当前是左儿子）或 `fa*2`（如果当前是右儿子）。使用 `countDown` 统计该子树。

4. **枚举距离。** 距离 `d` 从 1 到适当上限（完全二叉树高度约 30 层，向上拐入兄弟子树再往下走也增加 1 步，所以上限设为 65 足够）。对于每个 `d`：

```
L = countDown(2*x, d-1);       // 左子树方向
R = countDown(2*x+1, d-1);     // 右子树方向
U = countUp(x, d);             // 父亲方向
ans += L*R + L*U + R*U;
```

5. **加上单点路径。** 初始 `ans = 1`，计入 `(x, x)`。枚举完所有 `d` 后得到总路径数。

6. **使用长整型并注意边界。** `n`、`x` 以及函数内部的乘除运算都应使用 `long long`；遇到 `u > n` 时返回 0，防止越界；在父亲方向循环中当 `child == 1`（根节点）则停止向上。

通过以上方法，可以系统地解决完全二叉树路径中点计数问题，并避免因父亲方向处理不全或大数组引发的错误。
