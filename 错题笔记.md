# 算法错题笔记 — 位运算与 XOR 路径题型（D 题）

这份笔记针对 D 题（完全图中最小最大边权路径）的常见错误进行总结与分析，帮助理解异或路径的性质及最优构造。

## 题目梗概

在包含 `n` 个节点的完全图中，任意两点之间存在一条边，边权为两端节点编号的按位异或 `i ^ j`。一条路径的权值定义为该路径经过的所有边权中的最大值。给定两个节点 `l` 和 `r`，求一条从 `l` 到 `r` 的路径，使得该路径的最大边权尽可能小，并输出一条满足要求的路径。

## 常见错误

* **未理解异或边权的性质。** 有些解答直接遍历所有可能路径或贪心选择当前最小边权，忽视了异或操作的位性质。其实该问题的核心在于 `l ^ r` 的最高位决定了最优答案的下界。

* **缺少下界证明。** 很多解法只会提供一条可行路径，却不能解释其最优性。正确逻辑是：若设 `x = l ^ r`，`p` 为 `x` 的最高位对应的 2 的幂（即 `2^h`），则所有边权小于 `p` 的边在第 `h` 位均为 0，无法改变 `l` 和 `r` 在第 `h` 位的差异。故最优答案不可能小于 `p`。

* **错误逐位翻转。** 有些实现逐位将 `l` 变成 `r`，构造长路径 `l → l^2^k1 → l^2^k1^2^k2 → … → r`，虽然可行，但没有利用完全图任意两点有边的特点，且未压缩路径为最多 3 个节点。

* **未考虑直接连边情况。** 当 `l ^ r` 本身是 2 的幂时，意味着两点之间的异或值已经只有一个高位不同，此时直接路径 `(l, r)` 的边权等于最优答案，不必再引入中间点。若继续构造 `(l, mid, r)` 会导致中间点等于 `r`，无意义。

* **不理解为何最多 3 个点。** 部分解答无法解释为什么简单路径不会超过 3 个节点。实际上，只需一次翻转 `l` 和 `r` 的最高不同位即可消除这一位的差异，剩余低位差异可以通过直接跳转（完全图任意两点有边）一次性处理，因此最多只需 `(l, l^p, r)` 三个节点。

## 正确做法要点

1. **计算 `l ^ r` 的最高位。** 设 `x = l ^ r`，并找到其最高位对应的 2 的幂 `p`。可以通过循环左移或者利用内置函数找到最高位。

2. **判断是否直接连边。** 如果 `x == p`，说明 `l ^ r` 仅有一个位为 1，这条边权本身就是最优答案，输出 `l r` 即可。

3. **构造三点路径。** 否则构造中间节点 `mid = l ^ p`：

   - 第一条边 `(l, mid)` 的权值是 `l ^ mid = p`；
   - 第二条边 `(mid, r)` 的权值是 `(l ^ p) ^ r = (l ^ r) ^ p = x ^ p`，显然小于 `p`；
   - 因此整条路径的最大边权等于 `p`，达到最优下界。

4. **输出路径。** 如果是直接连边情况，输出两点；否则输出三点 `l mid r`。路径长度也即为输出的节点个数。

5. **理解证明。** 在答题时应写明为什么小于 `p` 的边无法改变最高位差异，以及为什么三点构造即可达到最优。

遵循以上要点，可以避免常见错误，正确求解这类位运算路径问题。